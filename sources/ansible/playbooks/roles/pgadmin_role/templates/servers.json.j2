{
    "Servers": {
      "1": {
        "Name": "{{ pg_admin_init_config_name }}", 
        "Group": "{{ pg_admin_init_config_group }}",
        "Host": "{{ postgres_hostname }}", 
        "Port": {{ postgres_port }},
        "MaintenanceDB": "postgres", 
        "Username": "{{ postgres_user }}",
        "SSLMode": "prefer"
      }
    }
}
  
